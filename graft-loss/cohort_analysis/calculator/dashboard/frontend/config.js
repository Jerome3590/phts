// API Configuration
const CONFIG = {
    // Update this with your API Gateway URL after deployment
    API_URL: 'https://YOUR_API_ID.execute-api.us-east-1.amazonaws.com/prod',
    
    // Endpoints
    ENDPOINTS: {
        METADATA: '/metadata',
        RISK: '/risk',
        COMPARISON: '/risk/comparison'
    }
};

